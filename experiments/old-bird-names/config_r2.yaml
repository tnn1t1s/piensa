# Rank 2 sensitivity experiment
# Same as old-bird-names but with LoRA rank=2

model: mlx-community/Mistral-7B-Instruct-v0.3-4bit
train: true
data: experiments/old-bird-names/data
adapter_path: old_birds_r2
iters: 624
batch_size: 1
num_layers: 16
learning_rate: 1e-5
lora_parameters:
  rank: 2
  scale: 20.0
  dropout: 0.0
