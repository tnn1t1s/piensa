# English LoRA Adapter Configuration
# Inherits from base.yaml

_base_: "base.yaml"

adapter:
  name: "lora_en"
  language: "en"

dataset:
  name: "tatsu-lab/alpaca"
  subset: null
  split: "train"

output_dir: "adapters/lora_en"
